import { Page } from "Modules/UiKit";
import { useTranslation } from "react-i18next";

export default function OS_COUNTRIES() {
  const { t } = useTranslation("");

  return (
    <Page>
      <div>OS_COUNTRIES</div>
    </Page>
  );
}
